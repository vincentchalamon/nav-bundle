{% extends 'base.html.twig' %}

{% block title %}Contacts list{% endblock %}

{% block stylesheets %}
    <style>
        form{text-align:left;max-width:50%;margin-left:20px;}
        input{margin:10px 0;display:inline-block;}
    </style>
{% endblock %}

{% block content %}
    <form action="{{ path('contact_search') }}" method="get">
        <input placeholder="N°" type="search" id="search" name="no" value="{{ app.request.query.get('no') }}"/>

        <button type="submit">Search</button>
    </form>

    <ul>
        {% for contact in contacts %}
            <li>
                <a href="{{ path('contact_edit', {'no': contact.no}) }}">{{ contact.no }}</a> - {{ contact.name }} ({{ contact.interventions.count }} interventions)
            </li>
        {% endfor %}
    </ul>
{% endblock %}

{% block footer %}
    <p><a href="{{ path('contact_create') }}">Create</a></p>
{% endblock %}
